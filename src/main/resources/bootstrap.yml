spring:
  cloud:
    config:
      uri: http://localhost:1000
  application:
    name: registry-service
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://configuration-service:1000